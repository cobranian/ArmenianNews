# Images bundlées en WebP 800 px — plan d'implémentation

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Ramener les images bundlées de 243 ko à ~63 ko de moyenne en les ré-encodant en WebP 800 px à la récolte, et convertir une fois le stock existant.

**Architecture :** Une règle d'encodage pure et testable dans `scripts/lib/image.mjs`, appelée par les deux scrapes locaux au moment où ils écrivent un fichier. Un script de migration convertit sur place les 1 438 images déjà présentes, sans aucun accès réseau. Le site accepte les deux extensions pour qu'aucun état intermédiaire ne casse l'affichage.

**Tech Stack :** Node 20+ (ESM), `sharp` (dépendance de développement, jamais en CI), Vite `import.meta.glob`, `node --test`.

**Spec :** `docs/superpowers/specs/2026-08-05-images-webp-800-design.md`

## Global Constraints

- **Cible : WebP, 800 px de large au maximum, qualité 78.** Valeurs exportées comme constantes, jamais écrites en dur ailleurs.
- **Ne jamais agrandir.** Une image plus petite que la cible garde sa taille.
- **Les planchers de taille restent AVANT l'encodage**, sur le buffer reçu du réseau : **10 000 octets** dans `ig-scrape.mjs`, **15 000 octets** dans `fb-scrape.mjs`. Ce sont deux valeurs différentes et existantes — ne pas les uniformiser.
- **`MIN_IMAGE_WIDTH` (640) ne change pas.** `pickImage` continue de choisir la source ; seul ce qu'on écrit change.
- **Les globs de `Social.jsx` acceptent `{jpg,webp}` définitivement**, pas seulement pendant la transition.
- **`sharp` est une `devDependency`.** Les deux scrapes sont locaux et manuels ; ni le build ni la CI ne doivent en dépendre.
- **Périmètre strict : `src/data/ig/` et `src/data/fb/` uniquement.** Les vignettes de presse sont hotlinkées ou passent par `wsrv.nl` — ne pas y toucher.
- **Le dépôt écrit ses commentaires en français**, dans la voix des fichiers voisins : on documente le *pourquoi* et les pièges silencieux, pas le *quoi*.
- **Contrôles qui doivent passer à chaque commit :** `npm test` (162 tests aujourd'hui, ce plan en ajoute), `npm run lint` (**0 erreur et exactement 5 avertissements connus** — un sixième est une régression).
- **Ce dossier est son propre dépôt git.** Vérifier `git rev-parse --show-toplevel` avant tout commit : le dossier parent est un autre dépôt.

---

### Task 1 : La règle d'encodage

**Files:**
- Create: `scripts/lib/image.mjs`
- Create: `test/image.test.mjs`
- Modify: `package.json` (ajout de `sharp` en `devDependencies`)

**Interfaces:**
- Consumes: rien (premier maillon)
- Produces:
  - `TARGET_WIDTH = 800` (number)
  - `TARGET_QUALITY = 78` (number)
  - `targetWidth(sourceWidth: number, max = TARGET_WIDTH) → number` — pure
  - `encode(buf: Buffer, opts?: { width?: number, quality?: number }) → Promise<Buffer>`

- [ ] **Step 1 : Installer `sharp` en dépendance de développement**

```bash
npm install --save-dev sharp
```

Vérifier ensuite que `package.json` place bien `sharp` dans `devDependencies` et **pas** dans `dependencies` :

```bash
node -e "const p=require('./package.json');console.log('dev:',!!p.devDependencies.sharp,'| prod:',!!(p.dependencies||{}).sharp)"
```

Attendu : `dev: true | prod: false`

- [ ] **Step 2 : Écrire le test qui échoue**

Créer `test/image.test.mjs` :

```js
import { test } from 'node:test'
import assert from 'node:assert/strict'
import sharp from 'sharp'
import {
  TARGET_QUALITY,
  TARGET_WIDTH,
  encode,
  targetWidth,
} from '../scripts/lib/image.mjs'

test('les constantes portent la cible retenue', () => {
  assert.equal(TARGET_WIDTH, 800)
  assert.equal(TARGET_QUALITY, 78)
})

test('targetWidth n agrandit jamais', () => {
  assert.equal(targetWidth(500), 500)
  assert.equal(targetWidth(500, 800), 500)
  assert.equal(targetWidth(799), 799)
})

test('targetWidth plafonne au maximum demande', () => {
  assert.equal(targetWidth(1080), 800)
  assert.equal(targetWidth(1440, 640), 640)
})

// Une largeur illisible retombe sur le maximum plutot que sur zero : une image
// de 0px de large serait un fichier vide, donc une tuile muette.
test('targetWidth retombe sur le maximum si la largeur est illisible', () => {
  assert.equal(targetWidth(undefined), 800)
  assert.equal(targetWidth(0), 800)
  assert.equal(targetWidth(NaN), 800)
  assert.equal(targetWidth(-10), 800)
})

// Image de synthese : ce test ne lit aucun fichier du depot et ne touche pas au
// reseau, comme ig-harvest.test.mjs.
const carre = (w, h) =>
  sharp({
    create: { width: w, height: h, channels: 3, background: { r: 200, g: 120, b: 40 } },
  })
    .jpeg()
    .toBuffer()

test('encode rend du WebP', async () => {
  const out = await encode(await carre(1200, 1500))
  assert.equal((await sharp(out).metadata()).format, 'webp')
})

test('encode reduit une image trop large et garde le rapport', async () => {
  const meta = await sharp(await encode(await carre(1200, 1500))).metadata()
  assert.equal(meta.width, 800)
  assert.equal(meta.height, 1000)
})

test('encode laisse une image deja petite a sa taille', async () => {
  const meta = await sharp(await encode(await carre(500, 620))).metadata()
  assert.equal(meta.width, 500)
  assert.equal(meta.height, 620)
})

test('encode accepte une largeur cible differente', async () => {
  const meta = await sharp(await encode(await carre(1200, 1200), { width: 640 })).metadata()
  assert.equal(meta.width, 640)
})

// Le script de migration doit pouvoir tourner une seconde fois sur son propre
// resultat : ecrit pour les seuls JPEG, il ne ferait rien apres la migration.
test('encode accepte du WebP en entree', async () => {
  const webp = await encode(await carre(1200, 1200))
  const meta = await sharp(await encode(webp, { width: 640 })).metadata()
  assert.equal(meta.format, 'webp')
  assert.equal(meta.width, 640)
})
```

- [ ] **Step 3 : Lancer le test pour vérifier qu'il échoue**

```bash
node --test test/image.test.mjs
```

Attendu : ÉCHEC, `Cannot find module '../scripts/lib/image.mjs'`

- [ ] **Step 4 : Écrire l'implémentation**

Créer `scripts/lib/image.mjs` :

```js
/**
 * La règle d'encodage des images bundlées, sortie des deux scrapes pour être
 * testable : ceux-ci pilotent un Chrome et ne sont pas exécutables en test.
 *
 * `sharp` est une dépendance de DÉVELOPPEMENT et doit le rester. Les deux
 * scrapes sont manuels et locaux ; ni le build ni la CI n'appellent ce module.
 */
import sharp from 'sharp'

// La tuile rend autour de 300px, et la visionneuse fait `min(1040px, 94vw)` :
// sur un téléphone de 390px elle affiche 367px, soit 734 en rétine. 800 couvre
// les deux. Descendre à 640 agrandirait l'image de 15 % exactement là où le
// lecteur la regarde de près.
export const TARGET_WIDTH = 800

// Mesuré sur 24 images réparties sur toute la distribution des largeurs du
// pool : 243 → 63 ko de moyenne, indiscernable de l'original à la taille
// d'affichage réelle, y compris sur une gravure manuscrite fine.
export const TARGET_QUALITY = 78

/**
 * La largeur de sortie. **N'agrandit jamais** : une image déjà à 500px reste à
 * 500px — agrandir coûterait des octets pour des pixels inventés.
 *
 * Une largeur illisible retombe sur le maximum, pas sur zéro : `sharp` traite
 * `width: 0` comme une erreur, et une image absente se lit comme une panne de
 * réseau alors que c'est un en-tête qu'on n'a pas su lire.
 */
export function targetWidth(sourceWidth, max = TARGET_WIDTH) {
  if (!Number.isFinite(sourceWidth) || sourceWidth <= 0) return max
  return Math.min(sourceWidth, max)
}

/**
 * JPEG, PNG ou WebP en entrée ; WebP redimensionné en sortie.
 *
 * `withoutEnlargement` double `targetWidth` à dessein : la première est la règle
 * qu'on teste sans `sharp`, le second est le garde de `sharp` lui-même. Si
 * `metadata()` ne rend pas de largeur, `targetWidth` retombe sur le maximum et
 * c'est alors ce garde qui empêche l'agrandissement.
 */
export async function encode(buf, { width = TARGET_WIDTH, quality = TARGET_QUALITY } = {}) {
  const img = sharp(buf)
  const { width: source } = await img.metadata()
  return img
    .resize({ width: targetWidth(source, width), withoutEnlargement: true })
    .webp({ quality })
    .toBuffer()
}
```

- [ ] **Step 5 : Lancer le test pour vérifier qu'il passe**

```bash
node --test test/image.test.mjs
```

Attendu : 10 tests, 0 échec.

- [ ] **Step 6 : Vérifier que rien d'autre n'a bougé**

```bash
npm test 2>&1 | grep -E "^. (pass|fail)"
npm run lint 2>&1 | tail -2
```

Attendu : `pass 172`, `fail 0` (162 + 10) ; `0 errors, 5 warnings`.

- [ ] **Step 7 : Commit**

```bash
git add package.json package-lock.json scripts/lib/image.mjs test/image.test.mjs
git commit -m "feat(images): la regle d encodage WebP 800px, testable sans reseau

sharp entre en devDependency : les deux scrapes qui l appelleront sont
manuels et locaux, ni le build ni la CI n en dependent.

targetWidth est pure et testee a part parce que c est la seule decision du
module : n agrandir jamais. Une largeur illisible retombe sur le maximum
et non sur zero — sharp traite width: 0 comme une erreur, et une image
absente se lirait comme une panne reseau alors que c est un en-tete qu on
n a pas su lire.

Un test verifie que encode accepte du WebP en entree : le script de
migration a venir doit pouvoir tourner une seconde fois sur son propre
resultat, sans quoi le relancer a 640px ne ferait rien, en silence."
```

---

### Task 2 : Le site accepte les deux extensions

**Files:**
- Modify: `src/components/Social.jsx:52-62`
- Modify: `test/instagram-pool.test.mjs:35-41`

**Interfaces:**
- Consumes: rien de la tâche 1
- Produces: rien pour les tâches suivantes — cette tâche rend le site **tolérant** avant que le moindre `.webp` n'existe, pour qu'aucun état intermédiaire n'affiche une tuile muette.

- [ ] **Step 1 : Écrire le test qui échoue**

Dans `test/instagram-pool.test.mjs`, remplacer le test des lignes 35-41 par celui-ci **et** ajouter le second :

```js
test('aucune image d un post exclu ne traine dans src/data/ig', async () => {
  const fichiers = await readdir(new URL('../src/data/ig/', import.meta.url))
  for (const f of fichiers.filter((x) => /\.(jpg|webp)$/.test(x))) {
    const code = f.replace(/\.(jpg|webp)$/, '')
    assert.ok(!exclus.has(code), `${f} appartient a un post exclu — poids mort dans le bundle`)
  }
})

// Le glob de Social.jsx et le filtre ci-dessus doivent regarder les MEMES
// extensions. Verrouilles sur des jeux differents, le test passerait au vert
// sur des fichiers que le site ignore — il ne prouverait plus rien.
test('le filtre du test suit le glob de Social.jsx', async () => {
  const src = await readFile(new URL('../src/components/Social.jsx', import.meta.url), 'utf-8')
  assert.match(src, /data\/ig\/\*\.\{jpg,webp\}/)
})
```

`readFile` est **déjà** importé en tête de ce fichier (`import { readFile, readdir } from 'node:fs/promises'`) — vérifié, rien à ajouter.

- [ ] **Step 2 : Lancer le test pour vérifier qu'il échoue**

```bash
node --test test/instagram-pool.test.mjs
```

Attendu : ÉCHEC sur `le filtre du test suit le glob de Social.jsx` — `Social.jsx` porte encore `../data/ig/*.jpg`.

- [ ] **Step 3 : Écrire l'implémentation**

Dans `src/components/Social.jsx`, remplacer le bloc des lignes 52-62 par :

```jsx
// Les deux globs acceptent `{jpg,webp}` DÉFINITIVEMENT, pas seulement pendant la
// migration vers WebP : un `.jpg` qui survivrait à une conversion incomplète
// continue de s'afficher. Verrouiller sur `.webp` transformerait tout fichier
// oublié en tuile muette retombée sur son motif, sans message.
const IG_IMAGES = import.meta.glob('../data/ig/*.{jpg,webp}', {
  eager: true,
  query: '?url',
  import: 'default',
})
const igImg = Object.fromEntries(
  Object.entries(IG_IMAGES).map(([path, src]) => [
    path.split('/').pop().replace(/\.(jpg|webp)$/, ''),
    src,
  ]),
)
```

Le glob Facebook (lignes 43-50) accepte déjà `{jpg,jpeg,png,webp}` et se clave sur le nom de fichier complet : **ne pas y toucher**.

- [ ] **Step 4 : Lancer les tests pour vérifier qu'ils passent**

```bash
node --test test/instagram-pool.test.mjs
npm test 2>&1 | grep -E "^. (pass|fail)"
```

Attendu : `pass 173`, `fail 0`.

- [ ] **Step 5 : Vérifier que le site bâtit et affiche toujours**

```bash
npm run lint 2>&1 | tail -2
npm run build 2>&1 | tail -2
npm run check 2>&1 | tail -2
```

Attendu : `0 errors, 5 warnings` ; `✓ 2 vitrines bâties` ; `✓ toutes les pages sont conformes`.

Puis vérifier qu'aucune tuile n'a perdu son image — le dossier ne contient encore que des `.jpg`, le compte doit être identique à avant :

```bash
node -e "
const fs=require('fs');
const p=require('./src/data/instagram.json');
const sc=u=>String(u).match(/\/(?:p|reel|tv)\/([^/?]+)/)?.[1];
const dans=new Set(p.accounts.flatMap(a=>a.posts.map(x=>sc(x.url))));
const f=new Set(fs.readdirSync('src/data/ig').filter(x=>/\.(jpg|webp)\$/.test(x)).map(x=>x.replace(/\.(jpg|webp)\$/,'')));
console.log('posts sans image :', [...dans].filter(s=>!f.has(s)).length);
"
```

Attendu : `posts sans image : 1` (l'échec de téléchargement connu, `DWr7TYjjHd9`).

- [ ] **Step 6 : Commit**

```bash
git add src/components/Social.jsx test/instagram-pool.test.mjs
git commit -m "feat(images): le glob Instagram accepte jpg ET webp, definitivement

Pose la tolerance AVANT qu un seul .webp n existe, pour qu aucun etat
intermediaire de la migration n affiche une tuile muette.

Le glob accepte les deux extensions pour toujours, pas seulement le temps
de la transition : un .jpg qui survivrait a une conversion incomplete
continue de s afficher, la ou un verrou sur .webp le transformerait en
tuile retombee sur son motif, sans message.

Un test lie le filtre du fichier de test au glob du composant. Verrouilles
sur des jeux d extensions differents, le test « aucune image d un post
exclu ne traine » passerait au vert sur des fichiers que le site ignore —
il ne prouverait plus rien."
```

---

### Task 3 : Les deux scrapes écrivent du WebP

**Files:**
- Modify: `scripts/ig-scrape.mjs` (imports, `download()` ~277-288, nettoyage d'orphelins ~321-327)
- Modify: `scripts/fb-scrape.mjs` (imports, `PINNED_FILE` ~63, boucle de téléchargement ~357-375)

**Interfaces:**
- Consumes: `encode` de `scripts/lib/image.mjs` (tâche 1)
- Produces: des fichiers `.webp` dans `src/data/{ig,fb}/`, et `facebook.json.image` en `.webp` pour les récoltes futures

**Ces deux scripts ne sont pas exécutables en test** (ils pilotent un Chrome connecté). Leur vérification est la relecture, le lint, et une exécution réelle en tâche 5.

- [ ] **Step 1 : Modifier `scripts/ig-scrape.mjs` — l'import**

Ajouter après l'import de `./lib/ig-harvest.mjs` :

```js
import { encode } from './lib/image.mjs'
```

- [ ] **Step 2 : Modifier `download()` dans `scripts/ig-scrape.mjs`**

Remplacer la fonction (~lignes 277-288) par :

```js
async function download(p) {
  const src = pickImage(p.candidates)
  if (!src) throw new Error('no usable image candidate')
  const res = await fetch(src, {
    headers: { referer: 'https://www.instagram.com/', 'user-agent': 'Mozilla/5.0' },
  })
  if (!res.ok) throw new Error(`HTTP ${res.status}`)
  const buf = Buffer.from(await res.arrayBuffer())
  // LE PLANCHER RESTE ICI, sur le JPEG REÇU — jamais après l'encodage. C'est lui
  // qui distingue une vraie image d'une page d'erreur (il a attrapé
  // DWr7TYjjHd9, 9,6 ko). Déplacé après, une image légitime de 42 ko ramenée à
  // 8 ko serait rejetée comme corrompue : un faux négatif silencieux, la tuile
  // retombant sur son motif sans que rien ne l'explique.
  if (buf.length < 10000) throw new Error(`too small (${buf.length}B)`)
  const webp = await encode(buf)
  await writeFile(path.join(IG_DIR, `${p.shortcode}.webp`), webp)
  return webp.length
}
```

- [ ] **Step 3 : Modifier le nettoyage d'orphelins dans `scripts/ig-scrape.mjs`**

Remplacer la boucle (~lignes 321-327) par :

```js
// Balaie les DEUX extensions : verrouillé sur une seule, le nettoyage laisserait
// l'autre s'accumuler sans qu'aucun compte ne diverge — pendant la migration,
// puis indéfiniment pour toute image restée en JPEG.
const live = new Set(accounts.flatMap((a) => a.posts.map((p) => shortcodeOf(p.url))))
let dropped = 0
for (const file of await readdir(IG_DIR)) {
  const m = file.match(/^(.+)\.(?:jpg|webp)$/)
  if (!m || live.has(m[1])) continue
  await unlink(path.join(IG_DIR, file))
  dropped++
}
```

- [ ] **Step 4 : Modifier `scripts/fb-scrape.mjs` — l'import et le fichier épinglé**

Ajouter l'import (après les autres imports locaux) :

```js
import { encode } from './lib/image.mjs'
```

Puis remplacer la ligne 63 :

```js
const PINNED_FILE = `${PINNED_ID}.webp`
```

- [ ] **Step 5 : Modifier la boucle de téléchargement de `scripts/fb-scrape.mjs`**

Remplacer la ligne qui compose `file` (~359) :

```js
  const file = isOwn ? PINNED_FILE : `${id}.webp`
```

Puis, dans le `try`, remplacer les trois lignes qui vont du plancher au log :

```js
    const buf = await res.buffer()
    // Le plancher reste AVANT l'encodage, sur les octets reçus de Facebook —
    // c'est lui qui reconnaît le substitut « accès refusé » de ~3 ko servi à une
    // session sans cookies. Il vaut 15 000 ici et 10 000 dans ig-scrape : deux
    // sources, deux substituts, deux seuils. Ne pas les uniformiser.
    if (buf.length < 15000) throw new Error(`too small (${buf.length}B)`)
    const webp = await encode(buf)
    await writeFile(path.join(FB_DIR, file), webp)
    const entry = { id, author: by, url, image: file }
    if (isOwn) pinned = entry
    else posts.push(entry)
    console.log(`  ✓ ${file} — ${by} (${(webp.length / 1024).toFixed(0)} KB)${isOwn ? ' ← épinglée' : ''}`)
```

- [ ] **Step 6 : Vérifier que les deux scripts se chargent et que le lint passe**

Le chargement d'un module vérifie sa syntaxe sans lancer Chrome :

```bash
node --check scripts/ig-scrape.mjs && node --check scripts/fb-scrape.mjs && echo "syntaxe OK"
npm run lint 2>&1 | tail -2
npm test 2>&1 | grep -E "^. (pass|fail)"
```

Attendu : `syntaxe OK` ; `0 errors, 5 warnings` ; `pass 173, fail 0`.

- [ ] **Step 7 : Vérifier qu'aucun `.jpg` codé en dur ne subsiste dans les deux scrapes**

```bash
grep -n "\.jpg" scripts/ig-scrape.mjs scripts/fb-scrape.mjs
```

Attendu : uniquement des occurrences dans des **commentaires** (l'explication de `dn-narek` dans `fb-scrape.mjs`) et dans le motif `(?:jpg|webp)` du nettoyage. Aucune écriture de fichier en `.jpg`. Si un commentaire nomme `dn-narek.jpg`, le corriger en `dn-narek.webp` — un commentaire qui décrit un nom de fichier périmé est le piège que `CLAUDE.md` documente sous « un commentaire qui justifie un manque doit mourir avec le manque ».

- [ ] **Step 8 : Commit**

```bash
git add scripts/ig-scrape.mjs scripts/fb-scrape.mjs
git commit -m "feat(images): les deux scrapes ecrivent du WebP 800px

Le plancher de taille reste AVANT l encodage dans les deux scripts, sur les
octets recus du reseau. C est lui qui distingue une vraie image d une page
d erreur — 10 ko cote Instagram (il a attrape DWr7TYjjHd9, 9,6 ko), 15 ko
cote Facebook, ou le substitut « acces refuse » servi a une session sans
cookies fait ~3 ko. Deux sources, deux substituts, deux seuils : ne pas les
uniformiser. Place apres l encodage, une image legitime de 42 ko ramenee a
8 ko serait rejetee comme corrompue, et la tuile retomberait sur son motif
sans explication.

Le nettoyage d orphelins balaie desormais les deux extensions. Verrouille
sur une seule, il laisserait l autre s accumuler sans qu aucun compte ne
diverge.

MIN_IMAGE_WIDTH ne bouge pas : pickImage choisit toujours la source la plus
petite >= 640px. Ce qui change n est pas ce qu on choisit, c est ce qu on
ecrit."
```

---

### Task 4 : Le script de migration, éprouvé sur un échantillon

**Files:**
- Create: `scripts/reencode-images.mjs`
- Modify: `package.json` (script `reencode`)

**Interfaces:**
- Consumes: `encode`, `TARGET_WIDTH`, `TARGET_QUALITY` de `scripts/lib/image.mjs` (tâche 1)
- Produces: un script utilisable à la demande. **Cette tâche ne convertit rien dans le dépôt** — elle livre l'outil et le passe au contrôle visuel. La conversion réelle est la tâche 5.

- [ ] **Step 1 : Écrire le script**

Créer `scripts/reencode-images.mjs` :

```js
/**
 * LOCAL-ONLY : ré-encode en WebP les images DÉJÀ bundlées.
 *
 * Aucun accès réseau. Les images sont sur le disque ; re-récolter 1 448 posts
 * prendrait des heures et risquerait un blocage Instagram pour des fichiers
 * qu'on possède déjà.
 *
 * Ce script n'est PAS un jetable. Le jour où la cible change — 640px si le
 * poids redevient un souci, 1040 si l'audience passe au bureau — c'est lui
 * qu'on relance.
 *
 *   node scripts/reencode-images.mjs --dry              # compte et pèse, n'écrit rien
 *   node scripts/reencode-images.mjs --sample <dossier> # écrit ailleurs, ne touche à rien
 *   node scripts/reencode-images.mjs                    # convertit sur place
 *   node scripts/reencode-images.mjs --width 640 --quality 75
 *
 * Il traite les `.webp` autant que les `.jpg`, et c'est délibéré. Écrit pour les
 * seuls JPEG il serait idempotent — agréable — mais INUTILISABLE une seconde
 * fois : après la migration il ne reste plus un seul `.jpg`, et le relancer à
 * 640 ne ferait rien du tout, en silence.
 *
 * Deux conséquences assumées. Un second passage ré-encode du WebP vers du WebP
 * et perd donc un peu de qualité : acceptable pour un changement de cible
 * délibéré, pas pour une routine. Et `encode()` n'agrandissant jamais, relancer
 * à 1040 après une migration à 800 NE REND PAS les pixels perdus — il faudrait
 * re-récolter. Ce script réduit, il ne restaure pas.
 */
import { mkdir, readdir, readFile, stat, unlink, writeFile } from 'node:fs/promises'
import path from 'node:path'
import { fileURLToPath } from 'node:url'
import { TARGET_QUALITY, TARGET_WIDTH, encode } from './lib/image.mjs'

const root = path.resolve(path.dirname(fileURLToPath(import.meta.url)), '..')

const DRY = process.argv.includes('--dry')
const opt = (nom) => {
  const i = process.argv.indexOf(`--${nom}`)
  return i === -1 ? null : process.argv[i + 1]
}
const nombre = (nom, def) => {
  const v = opt(nom)
  if (v === null) return def
  const n = Number(v)
  if (!Number.isFinite(n) || n <= 0) {
    console.log(`✗ --${nom} attend un nombre positif, reçu « ${v} »`)
    process.exit(1)
  }
  return n
}

const WIDTH = nombre('width', TARGET_WIDTH)
const QUALITY = nombre('quality', TARGET_QUALITY)
const LIMIT = nombre('limit', Infinity)
const SAMPLE = opt('sample')

const IMAGE = /\.(jpe?g|png|webp)$/i
const DOSSIERS = ['src/data/ig', 'src/data/fb']

console.log(`→ cible ${WIDTH}px q${QUALITY}${DRY ? ' (à sec)' : ''}${SAMPLE ? ` → ${SAMPLE}` : ''}`)

let avant = 0
let apres = 0
let n = 0
let echecs = 0

for (const rel of DOSSIERS) {
  const dir = path.join(root, rel)
  const fichiers = (await readdir(dir)).filter((f) => IMAGE.test(f)).sort()
  const aFaire = fichiers.slice(0, LIMIT === Infinity ? undefined : LIMIT)
  console.log(`\n${rel} : ${aFaire.length}/${fichiers.length} fichier(s)`)

  const sortie = SAMPLE ? path.join(root, SAMPLE, path.basename(rel)) : dir
  if (SAMPLE) await mkdir(sortie, { recursive: true })

  for (const f of aFaire) {
    const src = path.join(dir, f)
    const cible = path.join(sortie, f.replace(IMAGE, '.webp'))
    try {
      const buf = await readFile(src)
      const out = await encode(buf, { width: WIDTH, quality: QUALITY })
      avant += buf.length
      apres += out.length
      n++
      if (DRY) continue
      await writeFile(cible, out)
      // Ne supprimer que si on a bien écrit AILLEURS que sur la source : en mode
      // --sample l'original ne doit pas bouger, et une conversion sur place a
      // déjà écrasé le fichier quand les deux noms coïncident (.webp → .webp).
      if (!SAMPLE && path.resolve(cible) !== path.resolve(src)) await unlink(src)
    } catch (err) {
      echecs++
      console.log(`  ✗ ${f} : ${err.message} — laissé tel quel`)
    }
  }
}

const mo = (b) => (b / 1048576).toFixed(1)
console.log(`\n${n} image(s) encodée(s)${echecs ? `, ${echecs} échec(s)` : ''}`)
console.log(`  avant : ${mo(avant)} Mo  (moyenne ${(avant / n / 1024).toFixed(0)} ko)`)
console.log(`  après : ${mo(apres)} Mo  (moyenne ${(apres / n / 1024).toFixed(0)} ko)`)
console.log(`  réduction : ${(100 - (100 * apres) / avant).toFixed(1)} %`)

// facebook.json se clave sur le NOM DE FICHIER complet (Social.jsx : `fbImg[p.image]`),
// contrairement au pool Instagram qui se clave sur le shortcode. Renommer les
// fichiers sans réécrire ce champ laisserait les dix-huit tuiles Facebook
// retomber sur leur motif — un JSON valide, un mur muet, et rien pour le dire.
if (!DRY && !SAMPLE) {
  const P = path.join(root, 'src/data/facebook.json')
  const fb = JSON.parse(await readFile(P, 'utf-8'))
  let touches = 0
  fb.posts = fb.posts.map((p) => {
    if (!p.image || !IMAGE.test(p.image) || p.image.endsWith('.webp')) return p
    touches++
    return { ...p, image: p.image.replace(IMAGE, '.webp') }
  })
  await writeFile(P, JSON.stringify(fb, null, 2) + '\n')
  console.log(`✓ facebook.json : ${touches} champ(s) image mis à jour`)

  // Le fichier annoncé doit exister sur le disque. C'est le seul contrôle qui
  // attrape un renommage à moitié fait.
  let manquants = 0
  for (const p of fb.posts) {
    if (!p.image) continue
    try {
      await stat(path.join(root, 'src/data/fb', p.image))
    } catch {
      manquants++
      console.log(`  ✗ ${p.image} annoncé par facebook.json, absent de src/data/fb/`)
    }
  }
  if (manquants) {
    console.log(`✗ ${manquants} image(s) annoncée(s) et absente(s) — le mur Facebook serait troué`)
    process.exit(1)
  }
}
```

- [ ] **Step 2 : Ajouter le script à `package.json`**

Dans `"scripts"`, après `"ig-select"` :

```json
    "reencode": "node scripts/reencode-images.mjs",
```

- [ ] **Step 3 : Vérifier la syntaxe et le lint**

```bash
node --check scripts/reencode-images.mjs && echo "syntaxe OK"
npm run lint 2>&1 | tail -2
```

Attendu : `syntaxe OK` ; `0 errors, 5 warnings`.

- [ ] **Step 4 : Passage à sec sur tout le stock**

```bash
node scripts/reencode-images.mjs --dry
```

Attendu : ~1 456 images (1 438 Instagram + 18 Facebook), une réduction annoncée **entre 70 et 78 %**, 0 échec. Si la réduction sort de cette fourchette, s'arrêter et comprendre pourquoi avant d'écrire quoi que ce soit — la mesure de conception donnait −74 % sur un échantillon représentatif.

- [ ] **Step 5 : Le contrôle visuel sur les CAS DURS**

Le bijou testé pendant la conception est le cas *facile* pour WebP : du dégradé et de la texture. Le cas dur, ce sont les images **à texte plat** — cartons d'annonce, affiches d'événements, vignettes titrées — où WebP peut faire baver les contours.

Écrire l'échantillon ailleurs, sans toucher au dépôt :

```bash
SP="$(node -e "console.log(process.env.TMPDIR||process.env.TEMP||'/tmp')")/webp-check"
node scripts/reencode-images.mjs --sample "$SP"
```

Puis identifier trois images à texte plat. Elles se repèrent au poids élevé pour une largeur modeste (le texte net compresse mal en JPEG) :

```bash
node -e "
const fs=require('fs'),path=require('path');
const d='src/data/ig';
const r=fs.readdirSync(d).filter(f=>/\.(jpg|webp)\$/.test(f))
  .map(f=>({f,s:fs.statSync(path.join(d,f)).size}))
  .sort((a,b)=>b.s-a.s).slice(0,10);
r.forEach(x=>console.log((x.s/1024).toFixed(0)+' ko', x.f));
"
```

Ouvrir côte à côte l'original et sa conversion pour trois de ces fichiers, **à la taille d'affichage réelle** (600 px de large, la tuile sur écran rétine), et juger la lisibilité du texte. Ne pas juger à 100 % de zoom : ce n'est pas ainsi que le lecteur les voit.

**C'est un vrai portillon.** Si le texte bave à q78, remonter la qualité (`--quality 85`) et re-mesurer le gain avant de continuer. Consigner la décision.

- [ ] **Step 6 : Nettoyer l'échantillon**

```bash
rm -rf "$SP"
```

- [ ] **Step 7 : Commit du script seul**

```bash
git add scripts/reencode-images.mjs package.json
git commit -m "feat(images): scripts/reencode-images.mjs, la migration sans reseau

Convertit sur place les images deja bundlees. Aucun acces reseau : elles
sont sur le disque, et re-recolter 1 448 posts prendrait des heures pour
risquer un blocage Instagram sur des fichiers qu on possede.

Il traite les .webp autant que les .jpg. Ecrit pour les seuls JPEG il
serait idempotent, mais inutilisable une seconde fois : apres la migration
il ne reste plus un seul .jpg, et le relancer a 640 ne ferait rien, en
silence. Deux consequences assumees, documentees en tete : un second
passage perd un peu de qualite (WebP vers WebP), et relancer a 1040 apres
800 ne rend pas les pixels perdus — ce script reduit, il ne restaure pas.

Il reecrit aussi facebook.json, qui se clave sur le NOM DE FICHIER complet
la ou le pool Instagram se clave sur le shortcode. Renommer les fichiers
sans toucher ce champ laisserait les dix-huit tuiles Facebook retomber sur
leur motif : un JSON valide, un mur muet, rien pour le dire. Le script
verifie donc apres coup que chaque image annoncee existe sur le disque, et
sort en erreur sinon.

--sample ecrit ailleurs sans toucher aux originaux : c est ce qui permet
le controle visuel avant de convertir quoi que ce soit.

Ce commit ne convertit rien."
```

---

### Task 5 : La migration du stock

**Files:**
- Modify: `src/data/ig/*` (~1 438 fichiers), `src/data/fb/*` (18 fichiers), `src/data/facebook.json`

**Interfaces:**
- Consumes: `scripts/reencode-images.mjs` (tâche 4), le glob tolérant (tâche 2)
- Produces: un dépôt dont les images bundlées sont en WebP 800 px

- [ ] **Step 1 : Relever l'état AVANT**

```bash
du -sh src/data/ig src/data/fb
ls src/data/ig | wc -l
ls src/data/fb | wc -l
```

Noter les trois valeurs — elles servent de témoin à l'étape 4.

- [ ] **Step 2 : Convertir**

```bash
npm run reencode
```

Attendu : ~1 456 images, 0 échec, `✓ facebook.json : 18 champ(s) image mis à jour`, et **aucune ligne** « annoncé par facebook.json, absent ».

- [ ] **Step 3 : Vérifier qu'aucun `.jpg` ne subsiste**

```bash
ls src/data/ig src/data/fb | grep -c "\.jpg$" || echo "0 jpg restant"
```

Attendu : `0 jpg restant`.

- [ ] **Step 4 : Vérifier les comptes et le poids**

```bash
du -sh src/data/ig src/data/fb
node -e "
const fs=require('fs');
const p=require('./src/data/instagram.json');
const sc=u=>String(u).match(/\/(?:p|reel|tv)\/([^/?]+)/)?.[1];
const dans=new Set(p.accounts.flatMap(a=>a.posts.map(x=>sc(x.url))));
const f=new Set(fs.readdirSync('src/data/ig').filter(x=>/\.(jpg|webp)\$/.test(x)).map(x=>x.replace(/\.(jpg|webp)\$/,'')));
console.log('images :', f.size, '| posts sans image :', [...dans].filter(s=>!f.has(s)).length);
const fb=require('./src/data/facebook.json');
console.log('facebook : ', fb.posts.filter(x=>x.image).length, 'annoncees |', fs.readdirSync('src/data/fb').length, 'sur disque');
"
```

Attendu : `src/data/ig` autour de **82 Mo** (contre 319), le **même nombre d'images** qu'à l'étape 1, `posts sans image : 1` (l'échec connu `DWr7TYjjHd9`), et 18 = 18 côté Facebook.

**Si le poids ne tombe pas sous 120 Mo**, s'arrêter : la conversion n'a pas mordu là où on croit.

- [ ] **Step 5 : Les quatre contrôles du dépôt**

```bash
npm test 2>&1 | grep -E "^. (pass|fail)"
npm run lint 2>&1 | tail -2
npm run build 2>&1 | tail -2
npm run check 2>&1 | tail -2
du -sh dist/ch dist/org
```

Attendu : `pass 173, fail 0` ; `0 errors, 5 warnings` ; `✓ 2 vitrines bâties` ; `✓ toutes les pages sont conformes` ; **~92 Mo par vitrine** (contre 325).

- [ ] **Step 6 : Vérifier le rendu réel du mur**

Suivre le skill `verifier-le-rendu` du dépôt. Au minimum : lancer `npm run dev`, ouvrir le mur Instagram **et** le carrousel Facebook, et compter les tuiles qui affichent une image plutôt qu'un motif :

```bash
npm run dev
```

Dans le navigateur, sur `http://localhost:5173/`, vérifier que les 90 tuiles Instagram et les 18 vignettes Facebook portent bien une photo. Une tuile retombée sur son motif signale un fichier que le glob ne trouve pas.

- [ ] **Step 7 : Commit**

```bash
git add src/data/ig src/data/fb src/data/facebook.json
git commit -m "perf(images): convertir les 1 456 images bundlees en WebP 800px

Migration du stock, sans reseau. Mesure avant / apres :

  src/data/ig/       319 Mo -> ~82 Mo
  dist/ par vitrine  325 Mo -> ~92 Mo
  image moyenne      243 ko -> ~63 ko
  visite complete    ~32 Mo -> ~8 Mo

La cause etait la mediane du stock : 1080px pour des tuiles affichees a
300, et 638 images de plus de 1080px portant 63 % du poids. pickImage
demande pourtant le plus petit format >= 640px, mais dans 47 % des cas
l API Instagram n offre aucun palier intermediaire et la regle retombe sur
le plein format.

facebook.json suit : ses dix-huit champs image passent en .webp, et le
script verifie que chaque fichier annonce existe.

Reversible d une commande : git revert rend les .jpg, qui restent dans
l historique."
```

---

### Task 6 : Mettre la documentation à jour

**Files:**
- Modify: `CLAUDE.md` (deux passages : le poids par vitrine, la description d'`ig-scrape`)
- Modify: `README.md` (procédure de rafraîchissement des murs)

**Interfaces:**
- Consumes: les mesures réelles de la tâche 5
- Produces: rien de code

- [ ] **Step 1 : Corriger le poids annoncé dans `CLAUDE.md`**

Le passage sur `import.meta.glob` annonce « ~325 Mo par vitrine au 5 août 2026 » et explique que ce chiffre est passé de 92 à 325 en une journée. Le remplacer par la mesure de la tâche 5, et **garder l'avertissement sur `Σ count`** — il reste vrai : la grandeur qui gouverne le poids est le nombre total de posts, et rien ne la surveille. Ajouter que l'ordre de grandeur par image est désormais **~55 ko et non 170 à 250**, donc que +100 posts coûtent maintenant ~6 Mo et non ~20.

- [ ] **Step 2 : Corriger la description d'`ig-scrape` dans `CLAUDE.md`**

Le paragraphe qui décrit `scripts/ig-scrape.mjs` mentionne « télécharge leurs images dans `src/data/ig/` ». Préciser qu'il les **ré-encode en WebP 800 px** via `scripts/lib/image.mjs`, et ajouter le piège du plancher : il reste avant l'encodage, sur les octets reçus, et le déplacer après ferait rejeter des images légitimes.

- [ ] **Step 3 : Ajouter le piège du glob dans `CLAUDE.md`**

Sous « À savoir », ajouter une puce : les deux globs de `Social.jsx` acceptent `{jpg,webp}` définitivement ; les verrouiller sur `.webp` transformerait tout fichier resté en JPEG en tuile muette retombée sur son motif, sans message. `test/instagram-pool.test.mjs` lie le filtre du test au glob du composant pour que les deux ne divergent pas.

**En ajoutant ces pièges, en chercher un périmé à retirer** — c'est la règle du fichier. Candidat : la mesure « 229 Ko de moyenne sur les 218 images du dépôt » citée dans `scripts/lib/ig-harvest.mjs`, qui date de trois chantiers et de trois ordres de grandeur.

- [ ] **Step 4 : Mettre `README.md` à jour**

La procédure de rafraîchissement des deux murs doit mentionner que les images sont désormais ré-encodées à la récolte, et documenter `npm run reencode` pour un changement de cible.

- [ ] **Step 5 : Vérifier que la taille annoncée par `CLAUDE.md` est juste**

`CLAUDE.md` annonce sa propre taille en tête (« ~95 000 caractères, ~26 000 jetons »). La re-mesurer et la corriger si l'écart dépasse 2 000 caractères :

```bash
wc -c CLAUDE.md
```

- [ ] **Step 6 : Commit**

```bash
git add CLAUDE.md README.md scripts/lib/ig-harvest.mjs
git commit -m "docs: le poids des images apres passage en WebP 800px

Trois chiffres de CLAUDE.md etaient justes ce matin et faux depuis la
migration : le poids par vitrine, l ordre de grandeur par image, et le
cout d un compte ajoute au pool. L avertissement sur la somme des count
reste, lui : la grandeur qui gouverne le poids n a pas change, seulement
son coefficient.

Deux pieges ajoutes — le plancher de taille qui doit rester avant
l encodage, et les globs qui acceptent les deux extensions
definitivement. En echange, retrait de la mesure « 229 Ko sur 218 images »
de ig-harvest.mjs, qui datait de trois chantiers et de trois ordres de
grandeur."
```

---

## Self-Review

**1. Couverture de la spec**

| Exigence de la spec | Tâche |
|---|---|
| `scripts/lib/image.mjs`, `encode()`, jamais d'agrandissement | 1 |
| `sharp` en devDependency, jamais en CI | 1 |
| Plancher de 10 ko **avant** l'encodage (ig) | 3 |
| Plancher de 15 ko **avant** l'encodage (fb) | 3 |
| `MIN_IMAGE_WIDTH` inchangé | 3 (vérifié par omission, rappelé dans le message de commit) |
| Nettoyage d'orphelins sur les deux extensions | 3 |
| Glob Instagram `{jpg,webp}` définitif | 2 |
| `facebook.json.image` en `.webp` | 3 (récoltes futures) et 4 (stock) |
| `test/instagram-pool.test.mjs` suit | 2 |
| `scripts/reencode-images.mjs` reste dans le dépôt, traite les `.webp` | 4 |
| Migration sans réseau | 5 |
| Contrôle visuel sur les cas **durs** | 4 step 5 |
| Quatre contrôles + poids de `dist/` | 5 step 5 |
| Réversibilité par `git revert` | 5, message de commit |
| Périmètre : ni presse, ni `wsrv.nl` | Global Constraints |

Aucune exigence de la spec sans tâche. La section « Suite possible : PWA » est explicitement hors périmètre et n'a pas de tâche, ce qui est voulu.

**2. Placeholders** — aucun `TBD`, `TODO`, ni « ajouter la gestion d'erreur appropriée ». Chaque étape de code porte son code.

**3. Cohérence des types** — `encode(buf, { width, quality })` est défini en tâche 1 et appelé avec cette signature exacte en tâches 3 et 4. `targetWidth(sourceWidth, max)` n'est appelé que par `encode`. Les constantes `TARGET_WIDTH` / `TARGET_QUALITY` portent le même nom partout. `IMAGE` (le motif d'extension) est local à `reencode-images.mjs` ; `ig-scrape.mjs` utilise son propre `/^(.+)\.(?:jpg|webp)$/` parce qu'il capture le shortcode, ce que le premier ne fait pas — deux motifs, deux usages, pas une divergence.

**Un écart assumé entre les tâches 2 et 3** : le test « le filtre du test suit le glob de Social.jsx » vérifie la chaîne `data/ig/*.{jpg,webp}`. Si la tâche 3 changeait ce glob, le test tomberait — c'est exactement son rôle.
